# Core dependencies
manim>=0.19.0              # Manim Community Edition for mathematical animations
numpy>=1.22.0              # Required by Manim
scipy>=1.7.0
matplotlib>=3.5.0

# Hermes pipeline
mcp>=1.13.0                # Model Context Protocol server
pydantic>=2.8.0            # Data validation
python-dotenv>=1.0.1       # Environment variable management

# Text and LaTeX rendering
manimpango>=0.5.0          # Text rendering backend for Manim

# Development
pytest>=8.3.0
ruff>=0.8.0

# ──────────────────────────────────────────────
# System-level requirements (not pip-installable)
# ──────────────────────────────────────────────
#
# FFmpeg — Required for video rendering
#   Windows (Chocolatey):  choco install ffmpeg
#   Windows (Scoop):       scoop install ffmpeg
#   Windows (manual):      https://www.gyan.dev/ffmpeg/builds/
#   Linux (apt):           sudo apt-get install ffmpeg
#   macOS (Homebrew):      brew install ffmpeg
#
# LaTeX — Required for MathTex rendering
#   Windows:  MiKTeX  https://miktex.org/download
#   Linux:    sudo apt-get install texlive-full
#   macOS:    brew install --cask mactex
